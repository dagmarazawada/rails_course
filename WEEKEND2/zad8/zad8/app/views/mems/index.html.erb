<h1>Listing mems</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mems.each do |mem| %>
      <tr>
        <td><%= mem.name %></td>
        <td><%= mem.description %></td>
        <td><%= link_to 'Show', mem %></td>
        <td><%= link_to 'Edit', edit_mem_path(mem) %></td>
        <td><%= link_to 'Destroy', mem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mem', new_mem_path %>
